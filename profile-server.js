const{spawn}=require('node:child_process');
const crypto=require('node:crypto'),fs=require('node:fs'),http=require('node:http'),path=require('node:path');
const port=Number(process.env.PORT||3000),innerPort=port+1,dataRoot=path.resolve(process.env.DATA_DIR||path.join(__dirname,'data')),dataFile=path.join(dataRoot,'store.json'),dashboardRoutes=['/dashboard','/jobs','/messages','/network','/profile','/calendar','/notifications'];
const child=spawn(process.execPath,['server.js'],{cwd:__dirname,env:{...process.env,PORT:String(innerPort),DATA_DIR:dataRoot},stdio:'inherit'});
function blank(){return{users:[],companies:[],memberships:[],sessions:[],portfolioProjects:[],credentials:[],jobs:[],bids:[],conversations:[],messages:[],savedSearches:[],notifications:[],reviews:[]}}
function read(){fs.mkdirSync(dataRoot,{recursive:true});if(!fs.existsSync(dataFile))fs.writeFileSync(dataFile,JSON.stringify(blank()));return{...blank(),...JSON.parse(fs.readFileSync(dataFile,'utf8'))}}
function write(s){const t=`${dataFile}.${crypto.randomUUID()}.tmp`;fs.writeFileSync(t,JSON.stringify(s,null,2));fs.renameSync(t,dataFile)}
function clean(v,n=300){return String(v||'').trim().slice(0,n)}function id(p){return`${p}_${crypto.randomUUID()}`}function now(){return new Date().toISOString()}
function cookies(r){return Object.fromEntries(String(r.headers.cookie||'').split(';').map(x=>x.trim().split('=')).filter(x=>x.length===2))}
function context(r,s){const q=s.sessions.find(x=>x.token===cookies(r).trades_session&&x.expiresAt>now()),u=s.users.find(x=>x.id===q?.userId),m=s.memberships.find(x=>x.userId===u?.id),c=s.companies.find(x=>x.id===m?.companyId);return u&&c?{user:u,company:c}:null}
function body(r){return new Promise((ok,no)=>{let b='';r.on('data',x=>{b+=x;if(b.length>1e6)no(Error('large'))});r.on('end',()=>{try{ok(JSON.parse(b||'{}'))}catch{no(Error('json'))}})})}
function json(r,n,v){r.writeHead(n,{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});r.end(JSON.stringify(v))}
function send(r,n,v,type='text/plain; charset=utf-8',extra={}){r.writeHead(n,{'Content-Type':type,'Cache-Control':'no-store','X-Content-Type-Options':'nosniff',...extra});r.end(v)}
function validUrl(v){try{const u=new URL(v);return['http:','https:'].includes(u.protocol)?u.toString():''}catch{return''}}
function proxy(req,res,onJson){const p=http.request({hostname:'127.0.0.1',port:innerPort,path:req.url,method:req.method,headers:req.headers},x=>{if(!onJson){res.writeHead(x.statusCode,x.headers);return x.pipe(res)}let chunks=[];x.on('data',chunk=>chunks.push(chunk));x.on('end',()=>{const raw=Buffer.concat(chunks);if(x.statusCode>=200&&x.statusCode<300){try{const value=onJson(JSON.parse(raw.toString('utf8'))),output=Buffer.from(JSON.stringify(value));res.writeHead(x.statusCode,{...x.headers,'content-length':output.length});return res.end(output)}catch(error){console.error(error)}}res.writeHead(x.statusCode,x.headers);res.end(raw)})});p.on('error',()=>json(res,503,{error:'Application starting. Please retry.'}));req.pipe(p)}
const cityCoordinates={'dallas,tx':[32.7767,-96.797],'fort worth,tx':[32.7555,-97.3308],'arlington,tx':[32.7357,-97.1081],'plano,tx':[33.0198,-96.6989],'frisco,tx':[33.1507,-96.8236],'mckinney,tx':[33.1972,-96.6398],'irving,tx':[32.814,-96.9489],'garland,tx':[32.9126,-96.6389],'austin,tx':[30.2672,-97.7431],'houston,tx':[29.7604,-95.3698],'san antonio,tx':[29.4241,-98.4936],'columbus,oh':[39.9612,-82.9988]};
function locationKey(x){return`${clean(x.city,80).toLowerCase()},${clean(x.state,2).toLowerCase()}`}function distanceMiles(a,b){if(locationKey(a)===locationKey(b))return 0;const p=cityCoordinates[locationKey(a)],q=cityCoordinates[locationKey(b)];if(!p||!q)return Infinity;const rad=x=>x*Math.PI/180,dLat=rad(q[0]-p[0]),dLon=rad(q[1]-p[1]),z=Math.sin(dLat/2)**2+Math.cos(rad(p[0]))*Math.cos(rad(q[0]))*Math.sin(dLon/2)**2;return Math.round(3959*2*Math.atan2(Math.sqrt(z),Math.sqrt(1-z)))}
function inServiceArea(item,company,radius=company.serviceRadius||50){const distance=distanceMiles(item,company);return{matches:distance<=radius,distance}}
function date(v){return /^\d{4}-\d{2}-\d{2}$/.test(clean(v,10))?clean(v,10):''}function addDay(value){const d=new Date(`${value}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+1);return d.toISOString().slice(0,10)}function compact(value){return value.replaceAll('-','')}
function companyPublic(c){return{id:c.id,name:c.name,city:c.city,state:c.state,capabilities:c.capabilities,trades:c.trades,serviceRadius:c.serviceRadius||50}}
function notify(s,companyId,type,title,message,pathValue='',sourceId=''){if(sourceId&&s.notifications.some(x=>x.companyId===companyId&&x.type===type&&x.sourceId===sourceId))return;s.notifications.push({id:id('not'),companyId,type,title,message,path:pathValue,sourceId,readAt:null,createdAt:now()})}
function otherParty(job,companyId){return job.postingCompanyId===companyId?job.awardedCompanyId:job.postingCompanyId}
function searchMatches(search,job,company){return(!search.trade||clean(job.trade).toLowerCase().includes(clean(search.trade).toLowerCase()))&&inServiceArea(job,company,search.radiusMiles||company.serviceRadius||50).matches}
function reviewSummary(s,companyId){const reviews=s.reviews.filter(x=>x.reviewedCompanyId===companyId);return{count:reviews.length,overallRating:reviews.length?Number((reviews.reduce((n,x)=>n+x.overallRating,0)/reviews.length).toFixed(1)):null,recommends:reviews.filter(x=>x.recommends).length,reviews}}
function calendarParticipant(job,companyId){return['awarded','completed','closed'].includes(job.status)&&(job.postingCompanyId===companyId||job.awardedCompanyId===companyId)}
function calendarEvent(s,job,companyId){const partner=s.companies.find(x=>x.id===otherParty(job,companyId)),details=`${job.trade} project with ${partner?.name||'trade partner'}. ${job.calendarNotes||job.description||''}`.trim(),location=`${job.city}, ${job.state}`,endExclusive=addDay(job.endDate),params={text:job.title,dates:`${compact(job.startDate)}/${compact(endExclusive)}`,details,location},outlook={path:'/calendar/action/compose',rru:'addevent',subject:job.title,startdt:job.startDate,enddt:endExclusive,allday:'true',body:details,location};return{...job,partnerCompany:companyPublic(partner),googleUrl:`https://calendar.google.com/calendar/render?action=TEMPLATE&${new URLSearchParams(params)}`,outlookUrl:`https://outlook.live.com/calendar/0/deeplink/compose?${new URLSearchParams(outlook)}`,icsPath:`/api/calendar/jobs/${job.id}.ics`}}
function icsEscape(value){return String(value||'').replaceAll('\\','\\\\').replaceAll(';','\\;').replaceAll(',','\\,').replace(/\r?\n/g,'\\n')}
function calendarIcs(s,job,companyId){const event=calendarEvent(s,job,companyId);return['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Trades//Shared Project Calendar//EN','CALSCALE:GREGORIAN','METHOD:PUBLISH','BEGIN:VEVENT',`UID:${job.id}@trades.app`,`DTSTAMP:${new Date().toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,'')}`,`DTSTART;VALUE=DATE:${compact(job.startDate)}`,`DTEND;VALUE=DATE:${compact(addDay(job.endDate))}`,`SUMMARY:${icsEscape(job.title)}`,`DESCRIPTION:${icsEscape(`${job.trade} project with ${event.partnerCompany.name}. ${job.calendarNotes||job.description||''}`)}`,`LOCATION:${icsEscape(`${job.city}, ${job.state}`)}`,'END:VEVENT','END:VCALENDAR',''].join('\r\n')}
async function marketplaceApi(req,res,u){const s=read(),c=context(req,s);if(!c)return json(res,401,{error:'Sign in required'});if(req.method==='GET'&&u.pathname==='/api/notifications')return json(res,200,{notifications:s.notifications.filter(x=>x.companyId===c.company.id).sort((a,b)=>b.createdAt.localeCompare(a.createdAt)),unread:s.notifications.filter(x=>x.companyId===c.company.id&&!x.readAt).length});if(req.method==='POST'&&u.pathname==='/api/notifications/read-all'){s.notifications.filter(x=>x.companyId===c.company.id&&!x.readAt).forEach(x=>x.readAt=now());write(s);return json(res,200,{ok:true})}const readNotice=u.pathname.match(/^\/api\/notifications\/([^/]+)\/read$/);if(req.method==='POST'&&readNotice){const n=s.notifications.find(x=>x.id===readNotice[1]&&x.companyId===c.company.id);if(!n)return json(res,404,{error:'Notification not found.'});n.readAt=now();write(s);return json(res,200,{notification:n})}if(req.method==='GET'&&u.pathname==='/api/saved-searches')return json(res,200,{savedSearches:s.savedSearches.filter(x=>x.companyId===c.company.id)});if(req.method==='POST'&&u.pathname==='/api/saved-searches'){if(!c.company.capabilities.includes('subcontractor'))return json(res,403,{error:'Saved job searches are for subcontractor-capable companies.'});const b=await body(req),trade=clean(b.trade,100),radiusMiles=Math.max(5,Math.min(250,Number(b.radiusMiles)||c.company.serviceRadius||50)),search={id:id('sea'),companyId:c.company.id,name:clean(b.name,100)||`${trade||'All trades'} near ${c.company.city}`,trade,radiusMiles,createdAt:now()};s.savedSearches.push(search);write(s);return json(res,201,{savedSearch:search})}const deleteSearch=u.pathname.match(/^\/api\/saved-searches\/([^/]+)$/);if(req.method==='DELETE'&&deleteSearch){s.savedSearches=s.savedSearches.filter(x=>!(x.id===deleteSearch[1]&&x.companyId===c.company.id));write(s);return json(res,200,{ok:true})}if(req.method==='PUT'&&u.pathname==='/api/profile/service-area'){const b=await body(req),radius=Math.max(5,Math.min(250,Number(b.serviceRadius)||0));if(!radius)return json(res,400,{error:'Choose a service radius between 5 and 250 miles.'});c.company.serviceRadius=radius;write(s);return json(res,200,{company:companyPublic(c.company)})}const complete=u.pathname.match(/^\/api\/jobs\/([^/]+)\/complete$/);if(req.method==='POST'&&complete){const job=s.jobs.find(x=>x.id===complete[1]);if(!job||job.postingCompanyId!==c.company.id)return json(res,403,{error:'Only the posting contractor can complete this job.'});if(job.status!=='awarded')return json(res,400,{error:'Only awarded jobs can be marked complete.'});job.status='completed';job.completedAt=now();notify(s,job.awardedCompanyId,'job_completed','Project marked complete',`${job.title} is complete. You can now leave a verified review.`,'/calendar',job.id);write(s);return json(res,200,{job})}const reviewRoute=u.pathname.match(/^\/api\/jobs\/([^/]+)\/reviews$/);if(req.method==='GET'&&reviewRoute){const job=s.jobs.find(x=>x.id===reviewRoute[1]);if(!job||!calendarParticipant(job,c.company.id))return json(res,403,{error:'Reviews are available only to project parties.'});return json(res,200,{reviews:s.reviews.filter(x=>x.jobId===job.id)})}if(req.method==='POST'&&reviewRoute){const job=s.jobs.find(x=>x.id===reviewRoute[1]),b=await body(req);if(!job||job.status!=='completed'||!calendarParticipant(job,c.company.id))return json(res,403,{error:'Reviews require a completed awarded job.'});if(s.reviews.some(x=>x.jobId===job.id&&x.reviewerCompanyId===c.company.id))return json(res,409,{error:'Your company already reviewed this project partner.'});const reviewedCompanyId=otherParty(job,c.company.id),ratings=['qualityRating','scheduleRating','communicationRating','professionalismRating'].map(k=>Math.max(1,Math.min(5,Number(b[k])||0)));if(ratings.some(x=>!x))return json(res,400,{error:'Rate every review category.'});const review={id:id('rev'),jobId:job.id,trade:job.trade,reviewerCompanyId:c.company.id,reviewedCompanyId,qualityRating:ratings[0],scheduleRating:ratings[1],communicationRating:ratings[2],professionalismRating:ratings[3],overallRating:Number((ratings.reduce((a,x)=>a+x,0)/ratings.length).toFixed(1)),recommends:Boolean(b.recommends),body:clean(b.body,1500),createdAt:now()};s.reviews.push(review);notify(s,reviewedCompanyId,'review_received','New verified review',`${c.company.name} reviewed your work on ${job.title}.`,'/profile',review.id);write(s);return json(res,201,{review})}return json(res,404,{error:'Marketplace route not found.'})}
async function calendarApi(req,res,u){const s=read(),c=context(req,s);if(!c)return json(res,401,{error:'Sign in required'});if(req.method==='GET'&&u.pathname==='/api/calendar')return json(res,200,{events:s.jobs.filter(job=>calendarParticipant(job,c.company.id)&&job.startDate&&job.endDate).map(job=>calendarEvent(s,job,c.company.id))});const award=u.pathname.match(/^\/api\/jobs\/([^/]+)\/award$/);if(req.method==='POST'&&award){const job=s.jobs.find(x=>x.id===award[1]),b=await body(req),bid=s.bids.find(x=>x.id===b.bidId&&x.jobId===job?.id),startDate=date(b.startDate),endDate=date(b.endDate);if(!job||job.postingCompanyId!==c.company.id)return json(res,403,{error:'Only the posting contractor can award this job.'});if(job.status!=='published'||!bid)return json(res,400,{error:'Choose a valid open bid.'});if(!startDate||!endDate||endDate<startDate)return json(res,400,{error:'Choose valid project start and end dates.'});Object.assign(job,{status:'awarded',awardedBidId:bid.id,awardedCompanyId:bid.biddingCompanyId,startDate,endDate,calendarNotes:clean(b.calendarNotes,1200),awardedAt:now()});s.bids.filter(x=>x.jobId===job.id).forEach(x=>{x.status=x.id===bid.id?'accepted':'declined';x.updatedAt=now()});let conversation=s.conversations.find(x=>x.companyIds.includes(job.postingCompanyId)&&x.companyIds.includes(job.awardedCompanyId));if(!conversation)s.conversations.push({id:id('cnv'),companyIds:[job.postingCompanyId,job.awardedCompanyId],jobId:job.id,createdAt:now()});notify(s,job.awardedCompanyId,'job_awarded','You were awarded a job',`${c.company.name} awarded you ${job.title}.`,'/calendar',job.id);write(s);return json(res,200,{event:calendarEvent(s,job,c.company.id)})}const update=u.pathname.match(/^\/api\/calendar\/jobs\/([^/.]+)$/);if(req.method==='PUT'&&update){const job=s.jobs.find(x=>x.id===update[1]),b=await body(req),startDate=date(b.startDate),endDate=date(b.endDate);if(!job||!calendarParticipant(job,c.company.id))return json(res,403,{error:'This project is not on your shared calendar.'});if(!startDate||!endDate||endDate<startDate)return json(res,400,{error:'Choose valid project start and end dates.'});job.startDate=startDate;job.endDate=endDate;job.calendarNotes=clean(b.calendarNotes,1200);job.scheduleUpdatedAt=now();notify(s,otherParty(job,c.company.id),'schedule_updated','Project schedule updated',`${c.company.name} updated the schedule for ${job.title}.`,'/calendar',`${job.id}:${job.scheduleUpdatedAt}`);write(s);return json(res,200,{event:calendarEvent(s,job,c.company.id)})}const download=u.pathname.match(/^\/api\/calendar\/jobs\/([^/.]+)\.ics$/);if(req.method==='GET'&&download){const job=s.jobs.find(x=>x.id===download[1]);if(!job||!calendarParticipant(job,c.company.id))return json(res,404,{error:'Calendar event not found.'});return send(res,200,calendarIcs(s,job,c.company.id),'text/calendar; charset=utf-8',{'Content-Disposition':`attachment; filename="trades-${job.id}.ics"`})}return json(res,404,{error:'Calendar route not found.'})}
async function profiles(req,res,u){const s=read(),c=context(req,s);if(!c)return json(res,401,{error:'Sign in required'});if(req.method==='PUT'&&u.pathname==='/api/profile/location'){const b=await body(req),city=clean(b.city,80),state=clean(b.state,2).toUpperCase();if(!city||state.length!==2)return json(res,400,{error:'Enter a city and two-letter state.'});c.company.city=city;c.company.state=state;write(s);return json(res,200,{company:companyPublic(c.company)})}const match=u.pathname.match(/^\/api\/profiles\/([^/]+)$/);if(match&&req.method==='GET'){const target=s.companies.find(x=>x.id===match[1]);if(!target||(!target.capabilities.includes('subcontractor')&&target.id!==c.company.id))return json(res,404,{error:'Subcontractor profile not found.'});return json(res,200,{company:companyPublic(target),portfolio:s.portfolioProjects.filter(x=>x.companyId===target.id),credentials:s.credentials.filter(x=>x.companyId===target.id).map(({privateDocumentUrl,...x})=>x),reviewSummary:reviewSummary(s,target.id),isOwner:target.id===c.company.id})}if(req.method==='POST'&&u.pathname==='/api/profile/portfolio'){if(!c.company.capabilities.includes('subcontractor'))return json(res,403,{error:'Enable subcontractor capability to publish completed work.'});const b=await body(req),title=clean(b.title,160),photoUrl=validUrl(b.photoUrl);if(!title||!photoUrl)return json(res,400,{error:'Add a project title and valid image URL.'});const p={id:id('por'),companyId:c.company.id,title,trade:clean(b.trade,100),city:clean(b.city,80),state:clean(b.state,2).toUpperCase(),completedAt:clean(b.completedAt,20),description:clean(b.description,1500),photoUrl,createdAt:now()};s.portfolioProjects.push(p);write(s);return json(res,201,{project:p})}if(req.method==='POST'&&u.pathname==='/api/profile/credentials'){if(!c.company.capabilities.includes('subcontractor'))return json(res,403,{error:'Enable subcontractor capability to publish credentials.'});const b=await body(req),type=['license','insurance','certification','bond'].includes(b.type)?b.type:'',name=clean(b.name,160);if(!type||!name)return json(res,400,{error:'Choose a credential type and name.'});const x={id:id('cred'),companyId:c.company.id,type,name,issuer:clean(b.issuer,160),credentialNumber:clean(b.credentialNumber,100),state:clean(b.state,2).toUpperCase(),expiresAt:clean(b.expiresAt,20),coverage:clean(b.coverage,160),notes:clean(b.notes,1000),verificationStatus:'self-reported',createdAt:now()};s.credentials.push(x);write(s);return json(res,201,{credential:x})}return json(res,404,{error:'Profile route not found.'})}
const server=http.createServer((req,res)=>{const u=new URL(req.url,`http://${req.headers.host||'localhost'}`);if(u.pathname.startsWith('/api/notifications')||u.pathname.startsWith('/api/saved-searches')||u.pathname==='/api/profile/service-area'||/^\/api\/jobs\/[^/]+\/(complete|reviews)$/.test(u.pathname))return marketplaceApi(req,res,u);if(u.pathname.startsWith('/api/calendar')||/^\/api\/jobs\/[^/]+\/award$/.test(u.pathname))return calendarApi(req,res,u);if(u.pathname.startsWith('/api/profile'))return profiles(req,res,u);if(req.method==='POST'&&u.pathname==='/api/jobs')return proxy(req,res,data=>{const s=read(),job=data.job;if(job)s.savedSearches.forEach(search=>{const company=s.companies.find(x=>x.id===search.companyId);if(company&&company.id!==job.postingCompanyId&&searchMatches(search,job,company))notify(s,company.id,'matching_job','New job matches your saved search',`${job.title} in ${job.city}, ${job.state} matches ${search.name}.`,'/jobs',`${search.id}:${job.id}`)});write(s);return data});if(req.method==='GET'&&u.pathname==='/api/dashboard'){const s=read(),c=context(req,s);return proxy(req,res,data=>{if(!c)return data;data.company.serviceRadius=c.company.serviceRadius||50;data.availableJobs=(data.availableJobs||[]).map(job=>({...job,distanceMiles:inServiceArea(job,c.company).distance})).filter(job=>job.distanceMiles<=(c.company.serviceRadius||50));data.directory=(data.directory||[]).map(company=>({...company,distanceMiles:inServiceArea(company,c.company).distance})).filter(company=>company.distanceMiles<=(c.company.serviceRadius||50));data.marketplaceLocation={city:c.company.city,state:c.company.state,serviceRadius:c.company.serviceRadius||50};data.unreadNotifications=s.notifications.filter(x=>x.companyId===c.company.id&&!x.readAt).length;return data})}if(dashboardRoutes.includes(u.pathname)){const html=fs.readFileSync(path.join(__dirname,'prototype','dashboard.html'),'utf8').replace('</body>','<script src="profile.js"></script><script src="location.js"></script><script src="calendar.js"></script><script src="marketplace.js"></script></body>');res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-cache'});return res.end(html)}return proxy(req,res)});
server.listen(port,'0.0.0.0',()=>console.log(`Trades profile gateway listening on ${port}`));for(const sig of['SIGTERM','SIGINT'])process.on(sig,()=>{child.kill(sig);server.close()});
