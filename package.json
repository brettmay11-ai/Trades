{
  "name": "trades-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Texas-first B2B marketplace for contractors and subcontractors",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check prototype/app.js"
  }
}
